spring:
  application:
    name: spring-webflux-demo
  webflux:
    hidden method:
      filter:
        enabled: true
  data:
    mongodb:
      uri: mongodb://localhost/test
      port: 27017

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2


logging:
  level:
    org:
      springframework:
        data: TRACE
  # tag::hidden[]

